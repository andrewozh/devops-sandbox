namespace: default

repo: http://git-server.default.svc.cluster.local/.git

chart:
  ingress:
    enabled: false

clouds:
  - name: local
    accounts:
      - name: killercoda
        environments:
          - name: demo
