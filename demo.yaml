namespace: default

repo: git://git-server.default.svc.cluster.local/

chart:
  ingress:
    enabled: false

clouds:
  - name: local
    accounts:
      - name: killercoda
        environments:
          - name: demo
