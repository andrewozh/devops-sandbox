namespace: default
repo: git://git-server.default.svc.cluster.local/

chart:
  ingress:
    enabled: false

clouds:
  - name: killercoda
    accounts:
      - name: demo
        environments:
          - name: shared

global:
  cloud: killercoda
  account: demo
  env: shared
